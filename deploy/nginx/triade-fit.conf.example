server {
    listen 80;
    server_name triade-api.testes-techcode.shop;


    # Depois de apontar o DNS, gere o certificado com Certbot e deixe-o
    # redirecionar HTTP para HTTPS.
    location / {
        proxy_pass http://127.0.0.1:3335;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # Margem para os metadados multipart; a API aceita arquivo de vídeo de até 150 MB.
        client_max_body_size 160M;
        proxy_read_timeout 180s;
    }
}
